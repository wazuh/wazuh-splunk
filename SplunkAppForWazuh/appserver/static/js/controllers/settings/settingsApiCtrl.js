define([
  '../module',
], function (
  controllers
  ) {
    'use strict';
    controllers.controller('settingsApiCtrl', function ($scope) {
      $scope.message = 'API'
      const epoch = (new Date).getTime()
      
    })
  });

